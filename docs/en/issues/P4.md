# P4 — Sprint close-out and housekeeping

> General index: [`../issues.md`](../issues.md) · API context: [`../api-documentation.md`](../api-documentation.md)
>
> 🇪🇸 Versión en español: [`../../es/issues/P4.md`](../../es/issues/P4.md)
>
> Do after [`P3.md`](./P3.md). Nothing blocking: these are the loose threads left over once P0–P3 were closed, plus the doc sync against the `git log`.

Contains: **EPIC 11** (close-out and final polish).

---

## EPIC 11 — Sprint close-out

### 11.1 — Dashboard responsive

**File:** `src/pages/Dashboard.tsx` (~line 158 after the P2 7.1 wiring).

**Problem:** the chart block still has a fixed `min-h-[350px] xl:min-h-[450px]`. On small screens it forces horizontal scroll and doesn't collapse well on mobile.

**Context:** this item was listed in `P2.md` §7.2 and was **deliberately left out** of the "resolve them" block because it falls under UI polish and the CLAUDE.md's deadline-execution rule says "only data connections, no refactor of UI that already works."

**Steps:**

- Swap the chart's `min-h-[350px] xl:min-h-[450px]` for a flexible height or `aspect-ratio`; allow it to collapse on mobile.
- Verify that the grids (`grid-cols-4` for metrics, `grid-cols-3` for statuses) fall back to 1–2 columns at intermediate breakpoints.
- Test at 360 px, 768 px, 1024 px, 1440 px without horizontal scroll.

**✅ Criterion:** the Dashboard renders without unintended scroll on mobile, tablet, laptop and desktop.

---

### 11.2 — Pre-existing lint errors in legacy files

**Status:** `npm run lint` currently reports 3 errors and 3 warnings. **None** come from the P0–P3 work; they are pre-existing debt in four files that weren't touched this session. The build stays green because `vite-plugin-checker` runs `tsc`, not `eslint`.

| File                                                    | Line | Rule                              | Notes                                                       |
| ------------------------------------------------------- | ---- | --------------------------------- | ----------------------------------------------------------- |
| `src/components/modals/EditDepartmentModal.tsx`         | 15   | `react-hooks/set-state-in-effect` | `setNewName(department.name)` inside `useEffect`.           |
| `src/components/ui/SessionTimeoutGuard.jsx`             | 22   | `react-hooks/set-state-in-effect` | `updateActivity()` called inside the mount `useEffect`.     |
| `src/components/ui/ProcessingModal.jsx`                 | 12   | `react-hooks/exhaustive-deps`     | Missing `steps.length` in the dependency array.             |
| `src/components/modals/TimeoutWarningModal.jsx`         | 2    | `no-unused-vars`                  | Unused `Icons` import.                                      |

**Steps:**

1. `EditDepartmentModal.tsx:14-16` — replace the `useEffect` syncing `newName` with `department.name` with either a derived value (`useMemo`) or a `key={department?.id}` on the modal to force a remount.
2. `SessionTimeoutGuard.jsx:20-22` — initialise `activityRef` with `Date.now()` in the `useState` initialiser instead of calling `updateActivity()` inside the mount effect. Consider migrating to TS (`.jsx` → `.tsx`) while you're in there — this falls under the migration rule in `CLAUDE.md`.
3. `ProcessingModal.jsx:12` — either add `steps.length` to the deps or refactor the interval with `useRef` so the timer doesn't depend on `steps`.
4. `TimeoutWarningModal.jsx:2` — drop the unused `Icons` import.

**✅ Criterion:** `npm run lint` returns no errors. Warnings acceptable if they're well-reasoned hook-dep choices.

---

### 11.3 — Manual browser verification of P2 + P3

**Problem:** the CLAUDE.md requires _"For UI or frontend changes, start the dev server and use the feature in a browser before reporting the task as complete."_ The 10 commits from this session (`0a34682`…`f6feba2`) passed `tsc` and `vite build`, but **none of them were exercised in `npm run dev`**.

**Flows to verify against a real backend:**

1. **Login → role-based redirect** (P2 8.3). After unifying casing to `ADMIN`/`USER`, any session in `localStorage` with the old shape (`role: "admin"`) is invalidated by `readStoredSession`. Confirm that:
   - A clean login as `ADMIN` redirects to `/admin`.
   - A clean login as `USER` redirects to `/dashboard`.
   - A persisted session in the old shape falls back to `/login` without blanking the page.
2. **Admin Panel** (P2 8.1 + 8.2):
   - `GET /admin/stats` is called and the 6 cards render real values (not the 8 invented ones).
   - Pagination on `GET /admin/users` works (query `?page=&limit=`).
   - Changing a role persists after the refetch (verify in Network).
   - The "Delete" button now actually fires `DELETE /admin/users/:id`, refetches, and the user disappears from the list.
   - The delete confirm dialog can be cancelled without firing the request.
3. **Sidebar** (P2 9.2): "Panel Admin" appears **inside the first group** (next to Dashboard) only when `user.role === "ADMIN"`. A `USER` doesn't see the link.
4. **Dashboard** (P2 7.1): `GET /dashboard` fires on mount; loading, error and empty states render correctly; the Y axis is no longer pinned at 14; the "YYYY-MM" months show up as "Ene"/"Feb".
5. **MetricCard as Link** (P2 7.3): clicking any Dashboard card navigates to its history route.
6. **LoginForm** (P3 10.2): with the password hidden, the icon is `Eye`; after clicking to reveal, the icon is `EyeOff`.
7. **Histories without `alert()`** (P3 10.5): force a failure on `DELETE /positions/:id`, `DELETE /vacancies/:id`, `PATCH /vacancies/:id/status`, `PUT /departments/:id`, `DELETE /departments/:id` — each error must show the **red inline banner** above the list, not a native popup.

**✅ Criterion:** the 7 flows verified in the dev server against a real backend, screenshots optional in `docs/en/last-changes.md`.

---

### 11.4 — Sync `docs/en/issues.md` and `issues/P{1,2,3}.md` with the `git log`

**Problem:** the backlog docs don't reflect the work done between `047169d` and `f6feba2`. Concrete examples today:

- `docs/en/issues.md` — P1 marked "🔨 In progress" but P1 4.4, 5.2, 6.2, 6.3, 6.4, 6.5 are all committed.
- Same file — P2 marked "🔨 In progress (9.1, 9.3 ✅)" — today 7.1, 7.3, 8.1, 8.2, 8.3, 9.2 are also closed.
- Same file — P3 marked "⏳ Pending" — today it's closed.
- The `[ ]` bullets per EPIC (`[ ] 4.4`, `[ ] 5.2`, `[ ] 6.2`…) are still stale.
- `docs/en/issues/P1.md`, `docs/en/issues/P2.md` — no "resolved" markers on the items already closed.
- `docs/en/issues/P3.md` — the "PARTIALLY RESOLVED" note on 10.4 is now fully resolved (P2 8.1 killed the `Promise<any>`).
- Every Spanish mirror in `docs/es/`.

**Steps:**

1. Flip each `[ ]` bullet to `[x]` with a reference to the commit that closed it.
2. Update the "Status" column of the index for P1 (✅), P2 (✅ except 11.1 moved to P4), P3 (✅).
3. Add the P4 row to the index (already done as part of this commit).
4. Mirror everything in `/es/`.

**✅ Criterion:** a new reader of the repo can tell the real status from `docs/en/issues.md` without having to run `git log`.

---

### 11.5 — Housekeeping: uncommitted `p2.md` audit

**Status:** the `p2.md` file at the repo root (an audit of the real state vs. the P2 backlog, dated 2026-07-09) is still **untracked**. It shows up in `git status` from the start of the session.

**Options:**

- **Move to `docs/en/audits/2026-07-09-p2-audit.md`** and commit. Upside: historical memory of what was pending when; useful for retrospectives.
- **Delete it.** It no longer carries new information now that P2 is closed.
- **Leave it as is.** No noise if nobody else clones the repo, but perpetuates an untracked file in `git status`.

**✅ Criterion:** clean `git status` (no untracked `p2.md`) or the file with its own home under `docs/`.

---

## Summary

| Item | Description                                        | Complexity | Blocks |
| ---- | -------------------------------------------------- | ---------- | ------ |
| 11.1 | Dashboard responsive                               | Medium     | No     |
| 11.2 | 3 lint errors in legacy modals/guards              | Low        | No     |
| 11.3 | Manual browser verification of P2+P3               | Medium     | No     |
| 11.4 | Sync `issues.md` + `P1/P2/P3.md` with `git log`    | Low        | No     |
| 11.5 | Uncommitted `p2.md`                                | Trivial    | No     |
